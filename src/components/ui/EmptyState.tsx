export default function EmptyState({ icon = '?' , title = 'Sin datos', description = '' }) { return <div style={{ display:'flex', flexDirection:'column', alignItems:'center', justifyContent:'center', padding:40, textAlign:'center', opacity:0.6 }}> <div style={{ fontSize:48, marginBottom:16, filter:'grayscale(100%) blur(1px)' }}>{icon}</div> <h3 style={{ fontSize:16, fontWeight:600, color:'var(--snow)', marginBottom:4 }}>{title}</h3> {description && <p style={{ fontSize:13, color:'var(--silver)' }}>{description}</p>} </div> }
